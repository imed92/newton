{% extends 'back/base_back.html.twig' %}

{% block title %}Modifier un casting{% endblock %}

{% block body %}
    <h1>Modifier un casting</h1>

    {{ include('back/casting/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a class="btn btn-primary float-end" href="{{ path('back_casting_index', {id: casting.movie.id}) }}">Retour à la liste</a>

    {{ include('back/casting/_delete_form.html.twig') }}
    
{% endblock %}
