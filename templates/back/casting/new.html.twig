{% extends 'back/base_back.html.twig' %}

{% block title %}Ajouter un casting{% endblock %}

{% block body %}
    <h1>Ajouter un casting</h1>
    <h2>{{ movie.title }}</h2>

    {{ include('back/casting/_form.html.twig') }}

    <a class="btn btn-primary float-end mb-3" href="{{ path('back_casting_index', {id: movie.id}) }}">Retour à la liste</a>
{% endblock %}
